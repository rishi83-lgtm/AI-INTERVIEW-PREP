import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import Layout from '../components/Layout';import api from '../services/api';
export default function History(){const [items,setItems]=useState([]);useEffect(()=>{api.get('/interviews/history').then(r=>setItems(r.data.data)).catch(()=>{})},[]);return <Layout title="Interview history"><div className="page"><div className="page-intro"><h2>Every practice session</h2><p>Review progress and revisit feedback from previous interviews.</p></div><section className="panel">{items.length?items.map(i=><div className="interview-row" key={i._id}><div><strong>{i.role}</strong><span className="muted"> · {i.type} · {new Date(i.createdAt).toLocaleDateString()}</span></div><div>{i.status==='completed'?<><b>{i.score}% </b><Link className="badge" to={`/interviews/${i._id}/result`}>View result</Link></>:<Link className="badge" to={`/interviews/${i._id}`}>Continue</Link>}</div></div>):<div className="empty">No interviews yet. Create your first mock interview.</div>}</section></div></Layout>}
